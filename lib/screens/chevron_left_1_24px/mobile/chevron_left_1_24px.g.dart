import 'package:flutter/material.dart';

class ChevronLeft124px extends StatelessWidget {
  final constraints;

  const ChevronLeft124px(
    this.constraints, {
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return;
  }
}
