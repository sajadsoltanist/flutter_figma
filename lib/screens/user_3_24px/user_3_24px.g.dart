import 'package:flutter/material.dart';

class User324px extends StatelessWidget {
  final constraints;

  const User324px(
    this.constraints, {
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return;
  }
}
